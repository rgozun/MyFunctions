# Features Metadata
def col_dtypes(df_input):
    ColDtypes = []
    ColDtypes = pd.DataFrame(df.dtypes)
    ColDtypes.reset_index(inplace=True)
    ColDtypes.columns = ['Feature','DataType']
    return(ColDtypes)
